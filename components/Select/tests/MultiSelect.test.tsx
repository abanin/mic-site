import { cleanup, render } from "@testing-library/react";

import { MultiSelect } from "hydrogen";

describe("MultiSelect test", () => {
  afterEach(cleanup);

  test("many options can be selected", () => {
    // render(<MultiSelect />);
  });
});
